import { ADD_FAILED_ORDER, REMOVE_FAILED_ORDER } from "./types";

const initialState = {
    postFailedData: [] //提交失败的订单数据要及时保存在缓存里，防止数据丢失【重要】！！！
}

export default orderReducer = (state = initialState, action) => {
    
}